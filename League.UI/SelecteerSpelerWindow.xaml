<Window x:Class="League.UI.SelecteerSpelerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:League.UI"
        mc:Ignorable="d"
        Title="SelecteerSpelerWindow" Height="150" Width="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="0" Name="SpelersListBox" Margin="5" SelectionChanged="SpelersListBox_SelectionChanged"/>
        <Button Grid.Row="1" Name="OkButton" Content="Ok" Margin="5" IsEnabled="False" Click="OkButton_Click"/>
    </Grid>
</Window>
